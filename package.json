{
  "name": "bot-md-ro",
  "version": "2.0.0",
  "description": "AI Telegram Bot with multiple features",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "telegraf": "^4.15.0",
    "@upstash/redis": "^1.28.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
